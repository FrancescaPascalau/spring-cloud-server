server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FrancescaPascalau/spring-cloud-server.git
        bootstrap: true
      enabled: true

  profiles:
    active: composite
